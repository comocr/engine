# include "SectionClass_0.h"

class SectionClass : public SectionClass_0
{
public:
	VOID Generate(BOOL bVDir, UINT nRLines, UINT sBeginX, UINT sEndX, UINT sBeginY, UINT sEndY, LayoutClass& Layout);
};
